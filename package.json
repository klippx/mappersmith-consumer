{
  "name": "mappersmith-consumer",
  "packageManager": "yarn@3.6.3",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -tpv run build && echo 'Build completed without errors.' || echo '\nBuild FAILED with errors!\n'",
    "clean": "yarn workspaces foreach -tpv run clean",
    "test": "yarn workspaces foreach -tpv run test && echo 'Test completed without errors.' || echo '\nTest FAILED with errors!\n'",
    "test:integration": "yarn workspaces foreach -tpv run test:live  && echo 'Integration completed without errors.' || echo '\nIntegration FAILED with errors!\n'"
  }
}
