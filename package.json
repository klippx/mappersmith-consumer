{
  "name": "mappersmith-consumer",
  "packageManager": "yarn@4.0.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:all": "yarn workspaces foreach -A run build && echo 'Build completed without errors.' || echo '\nBuild FAILED with errors!\n'",
    "clean:all": "yarn workspaces foreach -A run clean",
    "test:all": "yarn workspaces foreach -A run test && echo 'Test completed without errors.' || echo '\nTest FAILED with errors!\n'",
    "integration:all": "yarn workspaces foreach -A run test:live  && echo 'Integration completed without errors.' || echo '\nIntegration FAILED with errors!\n'"
  }
}
