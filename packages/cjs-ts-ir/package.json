{
  "name": "@mappersmith-consumer/cjs-ts-ir",
  "description": "Mappersmith consumer with commonjs and typescript using [import x = require y] syntax",
  "type": "commonjs",
  "scripts": {
    "build": "tsc --noEmit",
    "clean": "rm -rf dist/",
    "test:live": "tsc && node dist/index.js",
    "test": "vitest --run"
  },
  "dependencies": {
    "mappersmith": "2.43.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.25",
    "typescript": "^5.4.2",
    "vitest": "1.4.0"
  }
}
