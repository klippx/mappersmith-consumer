{
  "name": "@mappersmith-consumer/cjs-ts-cr",
  "description": "Mappersmith consumer with commonjs and typescript using [const x = require y] syntax",
  "type": "commonjs",
  "scripts": {
    "build": "tsc --noEmit",
    "clean": "rm -rf dist/",
    "test:live": "tsc && node dist/index.js",
    "test": "vitest --run"
  },
  "dependencies": {
    "mappersmith": "2.43.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3",
    "vitest": "1.2.1"
  }
}
