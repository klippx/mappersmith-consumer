{
  "name": "@mappersmith-consumer/esm-ts",
  "description": "Mappersmith consumer with esm and typescript, using named imports only from mappersmith, mappersmith/test, mappersmith/gateway and mappersmith/middleware",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "clean": "rm -rf dist/",
    "test:live": "tsc && node --experimental-specifier-resolution=node dist/index.js",
    "test": "vitest --run"
  },
  "dependencies": {
    "mappersmith": "2.43.0-beta.1"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vitest": "0.34.4"
  }
}
